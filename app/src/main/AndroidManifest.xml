<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".MyWorkoutApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.App.Starting">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.App.Starting">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- RemoteViews Widget Provider -->
        <receiver
            android:name="com.myworkoutroutine.widget.WorkoutAppWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.myworkoutroutine.widget.ACTION_SELECT_TIMER" />
                <action android:name="com.myworkoutroutine.widget.ACTION_START_TIMER" />
                <action android:name="com.myworkoutroutine.widget.ACTION_PAUSE_TIMER" />
                <action android:name="com.myworkoutroutine.widget.ACTION_RESET_TIMER" />
                <action android:name="com.myworkoutroutine.widget.ACTION_PREVIOUS_CARD" />
                <action android:name="com.myworkoutroutine.widget.ACTION_NEXT_CARD" />
                <action android:name="com.myworkoutroutine.widget.ACTION_UPDATE_WIDGET" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/workout_widget_info" />
        </receiver>
    </application>

</manifest>
