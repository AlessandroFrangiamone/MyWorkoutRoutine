package com.myworkoutroutine.widget

import androidx.glance.unit.ColorProvider

/**
 * Widget color constants that match the app theme colors defined in res/values/colors.xml
 * Using ColorProvider for Glance widgets with resource IDs
 */
object WidgetColors {
    val CarbonFiber = ColorProvider(R.color.widget_carbon_fiber)
    val CarbonFiberLight = ColorProvider(R.color.widget_carbon_fiber_light)
    val CarbonFiberDark = ColorProvider(R.color.widget_carbon_fiber_dark)
    val FitnessRed = ColorProvider(R.color.widget_fitness_red)
    val OffWhite = ColorProvider(R.color.widget_off_white)
    val Orange = ColorProvider(R.color.widget_orange)
    val Gray = ColorProvider(R.color.widget_gray)
}
